export enum InputType {
  TEXT = "text",
  EMAIL = "email",
  URL = "url",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  DROPDOWN = "dropdown",
  DATE = "date",
}

export enum Modality {
  IN_PERSON = "in_person",
  VIRTUAL = "virtual",
  HYBRID = "hybrid",
}